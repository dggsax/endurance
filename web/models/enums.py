import enum

class AffiliationEnum(enum.Enum):
    """Enum for representing affiliation of person"""

    StudentOrAlumni = "Student or Alumni"
    CommunityMember = "Other (Faculty, Staff, Community Member, etc.)"